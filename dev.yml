name: shopify-api-js

type: node

up:
  - node:
      version: v22.0.0
      package_manager: pnpm@10.16.1
      packages:
        - .
env:
  NODE_ENV: "development"

commands:
  server:
    run: pnpm dev
